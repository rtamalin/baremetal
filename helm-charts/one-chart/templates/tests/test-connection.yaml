apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "one-chart.fullname" . }}-test-connection"
  labels:
    {{- include "one-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "one-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
